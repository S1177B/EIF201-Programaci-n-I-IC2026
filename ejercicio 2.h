#ifndef FUNCIONES_H
#define FUNCIONES_H

// Función que invierte un arreglo sin modificar el original
// original[]: arreglo de entrada
// invertido[]: arreglo de salida (se llena dentro de la función)
// size: tamaño del arreglo
void invertirArreglo(int original[], int invertido[], int size);

#endif // FUNCIONES_H
